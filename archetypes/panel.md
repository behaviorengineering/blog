+++
date = '{{ .Date }}'
draft = false
title = '{{ replace .File.ContentBaseName "-" " " | title }}'
type = 'panel'

# Umbrella + project hub (shareable /categories/.../ lists). See .cursor/skills/site-cognitive-memetics-content/SKILL.md
categories = ['Cognitive-Memetics', 'Cube-Cows']

# Optional week / id before the title (same TLDR purple as sayings via .heading-code--tldr)
heading_code = ''

# Series label for the big hero on the detail page. Use `title` for a unique episode name (lists, prev/next, tab title).
project = 'Cube-Cows 🐮📈'

# Teaser for cards and single (often the full cube-cow storyboard). Add body below only if you need a long essay.
description = ''

# Cube-cows under content/cognitive-memetics/cows/: publish on Wednesdays (folder YYYY-MM-DD = that Wednesday).
+++
